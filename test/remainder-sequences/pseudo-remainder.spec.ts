
import { assert, expect } from 'chai';
import { describe } from 'mocha';
import { prem } from '../../src/remainder-sequences/pseudo-remainder';


describe('pseudo remainder', function() {
    /*
	it('should calculate the pseudo remainder correctly', 
	function() {
        {
            // This is the Wikipedia example
            let a = [[1], [0], [1], [0], [-3], [-3], [8], [2], [-5]];
            let b = [[3], [0], [5], [0], [-4], [-9], [21]];
            
            expect(prem(a,b,false)).to.eql(
                [[-15], [-0], [3], [0], [-9]]
            );
        }

        {
            let a = [[1], [-2], [0], [-4]];
            let b = [[1], [-3]];
            
            expect(prem(a,b,false)).to.eql(
                [[5]]
            );
        }
        
        {
            let a = [[1e10], [1e5]];
            let b = [[1e10], [1e5]];
            
            expect(prem(a,b,false)).to.eql(
                [[0]]
            );
        }
        {
            let a = [[1], [-3]];
            let b = [[5]];
            
            expect(prem(a,b,false)).to.eql(
                [[0]]
            );
        }
    });
    */
});
