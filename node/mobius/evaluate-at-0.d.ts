/**
 * Evaluates the given mobius function at x = 0.
 * @private
 * @param mobius - The mobius function
 * M(x) = (ax + b) / (cx + d) represented as [[a,b],[c,d]]
 */
declare function mobEvaluateAt0(mobius: number[][]): number;
export { mobEvaluateAt0 };
